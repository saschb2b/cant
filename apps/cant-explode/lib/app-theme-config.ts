import { createAppTheme } from "@cant/shared/lib/app-theme";

export const appThemeConfig = createAppTheme({
  labels: {
    betterLabel: "Correct",
    worseLabel: "Incorrect",
    badLabel: "Incorrect",
    goodLabel: "Correct",
  },
});
